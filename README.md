# PolIntel Data

Static `web/v1` data package for the PolIntel Lovable app.

Canonical manifest after GitHub Pages publication:

`https://rods1985.github.io/polintel-data/web/v1/manifest.json`

`POLINTEL_REMOTE_BUNDLE_URL` remains only a flagged fallback/seed source.
